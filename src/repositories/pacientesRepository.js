// const PacientesModel = require('../db/models/paciente');

// class PacientesRepository {
//     // find(arr = [], where = {}) {
//     //     return arr.filter(c => {
//     //         return c.visible == Boolean(Number(where.visible))
//     //     })
//     // }

//     async all(params = {}) {
//         const users = await PacientesModel.findAll();
//         return users;
//     }

//     findById(id) {
//         var paciente = PacientesModel();
//         return paciente.findById(id);
//     }
// }

// module.exports = PacientesRepository